# Buckets

| # | Bucket | Evidence from the repo | Size |
| --- | --- | --- | --- |
| 1 | ~~**Maps & GIS**~~ — ✅ done | See `maps_gis.md`. All 5 phases shipped: schema (`PointOfInterest` generalized from `Campsite`, category-based, + `Route`, both owned by their post), GPX parsing with ride stats, Nominatim geocoding, self-hosted PMTiles on R2, and frontend rendering (MapLibre + stats row) via correctly-scoped separate queries, not an inner join. Proven end-to-end on a real post (`kinzig-valley-loop`) with a real GPX and 5 geo-tagged points of interest. 135 unit tests passing, 78.63% coverage. Still done, not reopened — but the original PMTiles basemap was under-scoped to a Black Forest/Germany bbox; "Dream of North" exposed it as a gray map void outside Germany. Fixed via a data-asset swap (Europe-wide extract) plus a non-blocking coverage-check safeguard in `geo_sync.py`, see `gis_refactor.md`. | Large |
| 2 | **UI/UX refresh for 2026** — 🔄 Phases 1-4 done, Phase 5 (Hardening) open | Clean Blog was a 2016-era Bootstrap 5 template — now genuinely retired (confirmed via `grep -ri bootstrap static/ templates/` returning only a historical comment). See `ui_ux_refresh.md`: Phase 1 (token/type system), Phase 2 (dark mode — confirmed live via `data-theme`/`theme-toggle`), Phase 3 (editorial homepage), and Phase 4 (post-body block vocabulary — `[[route-map]]`/`[[gallery:]]`/`[[callout:]]`, confirmed real in `app/services/post_blocks.py`) have all shipped, verified against real commits/code, not just the doc's own prose (doc-drift audit correction — this row and the doc's own header previously, incorrectly, claimed "Phase 1 not started"). Phase 5 (accessibility/keyboard/screen-reader pass, 200%-zoom check, Lab-CWV re-measurement) remains genuinely open — its checkboxes in the doc are still unchecked. | Medium-Large |
| 3 | **Content discovery** | `Post.tags` field exists, nothing filters by it. Postgres full-text search was explicitly planned in the original concept doc — never wired to an endpoint or a search box. No pagination (fine at 2 posts, breaks at 20). | Medium |
| 4 | ~~**SEO & syndication**~~ — ✅ done | See `seo_beyond_basics.md`. `robots.txt` (AI training/citation crawler split — operator decision: block training, allow citation), `sitemap.xml`, `feed.xml` (RSS 2.0), per-post OpenGraph/Twitter-card meta tags, and `BlogPosting`/`Trip` JSON-LD structured data all shipped. Deliberately skipped `llms.txt` (evidence says AI crawlers overwhelmingly ignore it) and `FAQPage`/`HowTo` schema (deprecated by Google as of 2026) — see that doc's research summary. 386 tests passing, 96.49% coverage. Still open: live verification against a real link-preview debugger and Google's Rich Results Test (see that doc's Leftover). | Small-Medium |
| 5 | **Media pipeline maturity** — 🚧 Phases 1-3 done, Phase 4 pending final visual sign-off | See `media_storage_r2.md`. R2 media library live (browser→R2 uploads, off git); all 7 pre-existing files migrated to R2 and removed from git; `github_sync.py`'s webhook fetch simplified to `content/posts/` only. Verified in production, not just locally — this also caught a real live bug: the webhook only syncs data files, never redeploys app code, so Phase 2's async GPX-over-HTTPS fix sat undeployed for ~2 days while the webhook had already pushed R2-URL frontmatter, silently breaking both routed posts' maps/stats in production until a `make deploy` during Phase 4 verification caught and fixed it. All three posts confirmed `200` with correct R2 image/GPX URLs and working map/stats post-deploy. Still open: a human visual read-through on mobile+desktop (map rendering and layout are client-side JS, not remotely verifiable), and closing Phase 3's one deferred item (a real post created via Sveltia + live webhook push). Image optimization: **cover-image WebP resize/conversion now done** (see `fix_lcp_image_and_static_cache.md`, bucket #19 — doc-drift audit correction, this row previously said it "remains untouched"), verified via real 89-92% size reductions on production images. Responsive `srcset` and gallery-image optimization specifically remain untouched — separate, still-deferred gap. | Small |
| 6 | ~~SQLAdmin for structured data~~ — superseded | This bucket's premise no longer matches the plan: `maps_gis.md` settled on entering POIs/routes *through Sveltia, alongside their post* rather than a separate admin panel (see that doc's "owned by post" decision and its "also considered and set aside: SQLAdmin" note). `app/admin.py` remains a stub with no current feature depending on it shipping. Not deleting this row — keeping the record of a bucket that got resolved by a different decision rather than quietly vanishing. | — |
| 7 | **Ops hardening** — 🚧 backups scripted, restore untested | Backup verification (`monitoring_ops.md` Phase 4) is no longer gated on SQLAdmin — `review_17SEP2026.md`'s tech-debt register re-triggered it independent of that: `nearby_amenities` is 15k+ rows re-derived from a flaky public API (Overpass), not local git-tracked files, so "everything is re-derivable" stopped fully holding. `scripts/backup_db.py` (nightly `pg_dump` → gzip → R2, 14-day retention, `make backup`) is implemented and unit-tested with `boto3`/`subprocess` mocked. **Still open**: installing the host crontab entry on production and running one real restore test (`docs/dev/deployment.md` §6) — neither achievable from a sandbox with no production SSH/R2 access. No staging environment, no disaster-recovery drill ever run. | Small-Medium |
| 8 | **Performance & accessibility audit** | Never independently measured — no Lighthouse run, no axe accessibility scan. Mobile-first is claimed (by the template's pedigree) but not verified against your actual deployed pages with real content and images. | Small |
| 9 | **Reader engagement** | Currently one-way: no comments, no RSS-based following, no social share buttons. Worth naming since it's a real product decision (stay a personal log, or invite interaction) rather than an oversight — but currently zero infrastructure exists either way. | Medium |
| 10 | **Content volume vs. infra maturity** | 2 real posts exist. Everything from Sveltia to webhooks to Sentry to Dependabot is production-grade for a blog that currently has two entries. Worth naming plainly: the engineering is ahead of the content, and at some point publishing needs to catch up to what's been built to support it. | — |
| 11 | ~~**Security review (OWASP-informed)**~~ — ✅ Phases 1-5 done, Phase 6 partial | See `security_review_owasp.md`. Two real gaps fixed: an SSRF allowlist on the GPX-fetch path (a `gpx_file` URL is now only fetched when its host matches the configured R2 public URL, fails closed when unconfigured) and security-logging on both auth-failure paths (`/internal/resync`, the GitHub webhook — distinct, grep-able warnings with source IP, not silent 401s). Live production verification of the logging fix caught a genuine second bug: `source_ip` was always Caddy's internal Docker IP, not the real caller, because uvicorn wasn't started with `--proxy-headers` — fixed in `Dockerfile`, redeployed, re-verified showing the real public IP. SQLi and dependency scanning confirmed already solid; CSRF confirmed correctly out of scope for this header/HMAC-auth architecture, not overlooked. A weekly scheduled `pip-audit` workflow (`.github/workflows/scheduled-audit.yml`) re-checks `develop` independent of pushes. A CycloneDX SBOM is now generated + uploaded as a CI artifact (voluntary, not a CRA requirement for this project — checked directly). Credential-rotation discipline written down in `deployment.md`. Phase 6: OWASP ZAP baseline scan workflow written and ready (`zap-baseline.yml`, scoped to production's public homepage only); SonarCloud scoped but not wired up — both need one manual operator step (a `workflow_dispatch` click for ZAP; a SonarSource account + `SONAR_TOKEN` secret for SonarCloud) this agent had no `gh`/API credential to perform. 33 new/updated tests, 396 passing. | Medium |
| 12 | ~~**Search engine registration**~~ — ✅ code done, ❌ manual console steps pending | See `seo_search_console_registration.md`. Direct search confirmed `seo_beyond_basics.md` shipping `sitemap.xml` doesn't mean anything's actually been crawled yet. Code side: `Settings.google_site_verification` meta tag (empty-default, same non-required pattern as `sentry_dsn`), rendered in `base.html`'s `<head>`, live-verified in production HTML after deploy. What's genuinely left is a manual, external, multi-step console workflow — registering the Search Console property, submitting `sitemap.xml`, Request Indexing the 3 live posts, then all of Bing Webmaster Tools — tied to the operator's own Google/Bing accounts, not something this agent can do. 2 new tests, 398 passing. | Small |
| 13 | ~~**Elevation chart placement**~~ — ✅ done | See `fix_elevation_chart_below_map.md`. Operator-requested layout fix: swapped `route_stats.html`'s block order so the elevation chart renders below the map, not above it. Source order only — no CSS/JS changed, both blocks stay independently gated exactly as before. 1 new regression test. | Trivial |
| 14 | ~~**Restaurants missing from auto-discovered amenities**~~ — ✅ done | See `fix_amenity_restaurant_category.md`. `restaurant` already existed as a curated-POI category (editor dropdown + full marker icon/color support in `post-map.js`) but the Overpass auto-discovery query never fetched `amenity=restaurant` — a real backend gap, not a missing frontend feature. Added to `_TAG_TO_CATEGORY`/`_build_query`; fixed an existing test that used `amenity=restaurant` as its "unmapped tag" example, now incorrect by construction. `fast_food`/`cafe` deliberately left out of scope. 2 new tests. | Trivial |
| 15 | ~~**Peaks, cable cars, and a full amenity-taxonomy review**~~ — ✅ done | See `fix_peaks_cablecars_amenity_review.md`. Three parts, each at a different confidence level. Peaks: real tile inspection corrected the doc's own research — no `physical_point` layer exists in this basemap's actual schema; peaks live on `pois` as `kind="peak"`, and the native basemap already renders an icon+name label, so the new `peakElevationLayer()` is deliberately additive (elevation text only). Cable cars: real tile inspection at the actual Feldbergbahn coordinates found zero aerialway data anywhere — a real, closed "no," not built speculatively. New amenity categories: `cafe`/`bicycle_repair_station` added to Overpass auto-discovery (deliberately excluding `cafe`'s earlier stated exclusion — revisited with real reasoning, not silently reopened), verified against the real live Overpass API (6 cafes + 1 repair station found near Feldberg). **Regression #2, found live in production**: the elevation-text layer's own presence was silently suppressing the native peak icon+name label entirely, via an undocumented MapLibre cross-layer collision interaction — an "elevation" vs "ele" property-name hypothesis and a `symbol-sort-key` fix attempt were both checked directly against real data and disproven before the actual fix (`text-allow-overlap`/`text-ignore-placement`) was found and verified with a real headless browser against production, using the literal fixed function. **Regression #3, found live at a denser real location (Siebengebirge)**: fix #2 traded one problem for another — orphaned bare elevation numbers for minor, unnamed peaks the native layer doesn't consider prominent enough to label yet — fixed by adding the native layer's own per-feature `min_zoom` prominence gate to this layer's filter; verified at both a sparse (Feldberg) and dense (Siebengebirge) real location, with a separate isolation test distinguishing the fix's own correctness from ordinary MapLibre collision crowding (a real, distinct, honestly-disclosed residual trade-off, not chased further). 8 new tests total. | Trivial |
| 16 | ~~**Label priority fix + trail-differentiation review**~~ — ✅ done | See `fix_label_priority_and_trail_differentiation.md`. Companion to bucket #15. Real tile inspection over Siebengebirge/Königswinter found the exact mechanism behind "zero peaks, dozens of toponyms": one shared `places_locality` layer renders real settlement names and hyper-local cadastral field/forest names under an identical filter, distinguished only by `kind_detail`. Split into two layers — real names untouched, field names deferred behind `minzoom: 14` — rather than a blanket minzoom raise that would've also hidden real town names like Königswinter. Trail differentiation (Phase 0 only): checked both hiking and cycling route-relation data at the exact Rhine promenade where a real trail and cycling route both run — this basemap's schema carries neither, a real closed "no." **Same-day regression, found and fixed**: the first implementation mixed legacy and modern MapLibre filter syntax, which the style validator rejected outright, taking down the entire map (blank void) while `elevation-chart.js` kept working — reproduced and fixed with a real headless browser, before/after, plus a new regression test. 2 new tests total. | Trivial |
| 17 | ~~**Playwright e2e smoke tests**~~ — ✅ done | See `playwright_e2e_smoke_tests.md`. A third test tier (`tests/e2e/`): real Postgres/PostGIS, a real `uvicorn app.main:app` subprocess, a real Chrome — direct response to the filter-dialect regression, since every existing `post-map.js`/`elevation-chart.js` test only checks strings exist in the file, none execute the JS. Verified the new test actually closes the gap, not just adds count: checked out the exact broken commit and confirmed it fails with the real MapLibre `error` event strings from the incident, then confirmed it passes clean on the fix. **Two real, pre-existing bugs found and fixed along the way, neither hypothetical**: (1) the actual GitHub Actions CI has been failing on every push this session at the "Test" step — `ci.yml` never applied Alembic migrations to its ephemeral service container, so `make ci` run locally (against the already-migrated persistent dev DB) never caught it; confirmed via GitHub's own Actions API showing real past failures, reproduced locally against a genuinely fresh container, fixed with a new migration step. (2) This tier's own fixture seeding initially deleted the real local dev posts — `sync_posts()`'s reconciling delete isn't scoped to the directory it's given — restored immediately, then fixed at the design level with a dedicated, isolated `<db>_e2e` database created via the `postgres` maintenance DB. Flagged in the doc's Leftover that `tests/integration/` carries the identical exposure against the shared dev DB (found again while restoring content a second time), out of scope to fix here but documented concretely for a follow-up. 9 new e2e tests + `make e2e` target + CI wiring. | Medium |
| 18 | ~~**CyclOSM optional basemap layer**~~ — ✅ done | See `cyclosm_optional_layer.md`. First genuinely external third-party raster tile dependency in this project. Off-by-default toggle in `post-map.js`/`route_stats.html`, `transformRequest` scoped only to CyclOSM URLs. **Real work at three points, not just implementation**: (1) researched whether MapLibre GL JS supports a configurable tile timeout — it doesn't — then verified empirically with a real headless browser against a non-routable host that a caller-supplied `AbortSignal` via `transformRequest` genuinely aborts the fetch in this exact vendored build (v4.7.1); the Phase 2 timeout is built on that confirmed mechanism, not assumed documentation. (2) The existing amenities theme-swap carry-over pattern appends layers *after* the fresh base map — correct for route/amenities, wrong for CyclOSM, which must stay *below* them; fixed with a separate, correctly-ordered concat, verified live through a real theme-toggle click while active. (3) Verified against the doc's own real reference example (Hohenzollernbrücke, Köln): real street names, "EV15"/"EV4" EuroVelo badges, an "R17" route badge, a numbered junction, and a ferry line all rendered, plus a real total-failure simulation (every tile request aborted) confirming zero console/page errors and the fullscreen/theme toggles both staying interactive throughout. **Unrelated but real, blocking problem found and fixed along the way**: the local dev DB was two Alembic migrations behind head (`UndefinedColumn: routes.elevation_profile`), never surfaced before since no earlier check this session actually rendered a full local post page with a route — fixed with `alembic upgrade head`. 12 new tests. | Medium |
| 19 | **GitHub Actions CI failing again on "Apply database migrations"** — 🔴 found during a doc-drift audit, real, currently unresolved | The exact bug `playwright_e2e_smoke_tests.md` documented fixing is failing again — confirmed via the real GitHub Actions API (`"conclusion":"failure"` on every recent `develop` run, including this repo's own most recent commit). The original fix (a dedicated "Apply database migrations" step) still works correctly when reproduced locally — independently re-confirmed against both a brand-new `postgis/postgis:16-3.4` container and a completely fresh `uv sync`'d venv, both clean. Root cause of the *live* failure not found: local reproduction rules out the migrations/lockfile/dependencies themselves, but the actual GitHub Actions job log needs admin/write repo access (`403` here) to inspect further, and `act` isn't installed in this environment. Flagged honestly as unverifiable-from-here rather than guessed at — needs the operator's own Actions log access. | Small-Medium (once diagnosed) |
| 20 | ~~**Fix LCP (image delivery) + static-asset cache efficiency**~~ — ✅ done | See `fix_lcp_image_and_static_cache.md`. Two real PageSpeed Insights findings from a live run: LCP 15.1s, and the single largest saving on the whole report (2,547 KiB) from cache lifetimes, not image weight. **Finding 1**: new `app/services/image_processing.py` (Pillow resize+WebP) + `app/services/cover_image_sync.py` (fetch/process/upload, idempotent — an unchanged `cover_image` costs zero network calls on re-sync), wired into every real `sync_posts()` caller. Real, measured production results: `feldberg-summit-loop`'s cover image went from 2,607,625 bytes to 209,754 bytes (92% smaller) — confirmed by fetching the newly-uploaded real R2 object directly, not just trusting the sync log. Extracted the existing GPX SSRF allowlist into a shared `app/utils/url_safety.py`, reused rather than reimplemented for `cover_image`'s identical risk. **Finding 2**: static assets version-stamped (`app.main._compute_asset_version()`, a content hash, not a git SHA — changes the instant a file's bytes change, zero deploy-pipeline plumbing) with `?v=` on every CSS/JS reference; `Cache-Control: public, max-age=31536000, immutable` when `v=` is present, `no-cache` fallback otherwise — both verified live with `curl` against production. **Finding 3**: investigated with real data rather than guessed at — a genuine before/after Lighthouse comparison (3 runs each side, against the live production URL, since Google's own PSI API was rate-limited and its web UI is JS-rendered) showed LCP roughly halved and TBT down ~99%, both directly explained by Finding 1's real image-size reduction. **A genuine, unresolved anomaly surfaced and was reported honestly, not hidden**: FCP got measurably *worse*, consistently across 3 runs, isolated specifically to route/map pages (ruled out: server response time, a site-wide network/CDN artifact) — a plausible mechanism (the render-blocking `maplibre-gl.css`, present only on those pages) is documented but explicitly not confirmed. 26 new tests. | Medium |
